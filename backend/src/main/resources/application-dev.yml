spring:
  ai:
    openai:
      # Mock API key for development - Spring AI needs this to initialize
      # It won't be used since ai.enabled=false
      api-key: sk-mock-dev-key-1234567890123456789012345678901234567890

ai:
  enabled: false

fhir:
  server:
    url: https://snowstorm-training.snomedtools.org/fhir

snomed:
  branch: MAIN
  language: sv
  language-refset: 
  fallback-to-english: true
